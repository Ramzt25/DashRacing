# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: dash-racing-app

infra:
  provider: bicep

services:
  dash-racing-api:
    project: src
    host: containerapp
    language: ts
    docker:
      path: ../Dockerfile
      context: ..